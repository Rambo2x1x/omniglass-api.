{
  "name": "omniglass-monorepo",
  "version": "1.0.0",
  "description": "OmniGlass API Backend Repository",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  }
}
